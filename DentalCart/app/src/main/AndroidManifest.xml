<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dentalcart">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.ShowcategoriesActivity"></activity>
        <activity android:name=".UI.CartActivity" />
        <activity android:name=".UI.ShareappActivity" />
        <activity android:name=".UI.ProfileActivity" />
        <activity android:name=".UI.AddproductActivity" />
        <activity android:name=".UI.ShowreviewsActivity" />
        <activity android:name=".UI.FeedbackActivity" />
        <activity android:name=".UI.ShowproductActivity" />
        <activity android:name=".UI.ForgotpasswordActivity" />
        <activity android:name=".UI.RegisterActivity" />
        <activity android:name=".UI.LoginActivity" />
        <activity android:name=".UI.MainActivity" />
        <activity android:name=".UI.SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>